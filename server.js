// Compatibility entrypoint for the Vercel project imported from the original Node app.
// Production API functionality lives in /api/*.js serverless handlers.
module.exports = require("./api/meta");
